# 新生报到系统 4.0 - 使用说明书

## 目录

1. [系统简介](#1-系统简介)
2. [系统要求](#2-系统要求)
3. [安装部署](#3-安装部署)
4. [网页版使用指南](#4-网页版使用指南)
5. [小程序使用指南](#5-小程序使用指南)
6. [管理后台使用指南](#6-管理后台使用指南)
7. [常见问题](#7-常见问题)
8. [技术支持](#8-技术支持)

---

## 1. 系统简介

新生报到系统 4.0 是一款面向高校新生报到场景的信息采集与管理平台。系统在 3.0 版本基础上增加了管理员审核、宿舍分配和数据统计功能。新生需先通过手机号验证身份，填写个人信息并上传身份证正反面照片完成报到，等待管理员审核通过后可查看宿舍分配信息。系统支持网页版和微信小程序两种访问方式。

### 1.1 核心功能

| 功能 | 说明 |
|------|------|
| 手机号验证码登录 | 发送验证码到手机，验证后登录系统 |
| 学生信息采集 | 收集新生姓名、学号、手机号 |
| 身份证照片上传 | 上传身份证正反面照片，自动识别正反面 |
| 重复照片检测 | 检测是否上传同一张照片（MD5 + 感知哈希） |
| 报到记录管理 | 自动记录报到时间，关联学生信息 |
| 数据验证 | 前端+后端双重验证，确保数据准确性 |
| 管理员审核 | 审核学生报到信息，支持通过/驳回操作 |
| 宿舍分配 | 为审核通过的学生分配宿舍 |
| 宿舍信息查询 | 学生查看宿舍信息和室友列表 |
| 管理后台 | 查看、搜索、筛选、审核报到数据，管理宿舍信息 |
| 防重复报到 | 学号唯一性约束，防止重复提交 |

### 1.2 技术架构

| 层级 | 技术 |
|------|------|
| 后端 | Django 4.2 + Python 3.10+ |
| 数据库 | SQLite 3 |
| 图像处理 | OpenCV + NumPy |
| 网页前端 | HTML5 + CSS3 + JavaScript + CryptoJS |
| 小程序 | 微信小程序原生开发 |
| 版本控制 | Git + GitHub |

---

## 2. 系统要求

### 2.1 服务器端

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows / Linux / macOS |
| Python | 3.10 或以上 |
| Django | 4.2 |
| 数据库 | SQLite 3（内置） |
| 图像处理 | OpenCV + NumPy |
| 内存 | 最低 512MB |
| 硬盘 | 最低 200MB（含媒体文件存储） |

### 2.2 客户端

| 项目 | 要求 |
|------|------|
| 网页版 | Chrome / Edge / Firefox 等现代浏览器 |
| 小程序 | 微信 8.0 以上版本 |
| 网络 | 可访问服务器IP地址 |

---

## 3. 安装部署

### 3.1 下载代码

```bash
git clone https://github.com/sxr-star/sxr.git
cd sxr
```

### 3.2 安装依赖

**方式一：使用安装脚本（推荐）**

双击运行 `完整安装启动.bat`

**方式二：手动安装**

```bash
pip install django django-cors-headers opencv-python numpy pillow
```

### 3.3 初始化数据库

```bash
python manage.py migrate
```

### 3.4 创建管理员账号

```bash
python manage.py createsuperuser
```

按提示输入：
- 用户名（Username）
- 邮箱（Email，可选）
- 密码（Password）
- 确认密码（Password again）

### 3.5 启动服务器

**方式一：使用启动脚本（推荐）**

双击运行 `快速启动.bat`

**方式二：命令行启动**

```bash
python manage.py runserver 8000
```

### 3.6 访问系统

| 入口 | 地址 |
|------|------|
| 网页版 | http://127.0.0.1:8000/ |
| 管理后台 | http://127.0.0.1:8000/admin/ |
| API接口 | http://127.0.0.1:8000/api/ |

---

## 4. 网页版使用指南

### 4.1 访问网页版

在浏览器地址栏输入：`http://127.0.0.1:8000/`

### 4.2 页面说明

系统包含以下页面：

#### 4.2.1 验证页面

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"新生报到系统 - 身份验证" |
| 手机号输入框 | 输入11位手机号码 |
| 获取验证码按钮 | 点击发送验证码，60秒倒计时 |
| 验证码输入框 | 输入收到的4位数字验证码 |
| 登录按钮 | 点击验证并登录 |

#### 4.2.2 报到页面

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"欢迎使用新生报到系统" |
| 手机号显示区 | 自动填充当前登录用户的手机号，不可编辑 |
| 标签切换区 | 包含"报到登记"和"审核状态"两个标签 |
| 表单区 | 包含姓名、学号输入框和身份证照片上传区域 |
| 提交按钮 | 点击提交报到信息 |
| 结果区 | 显示报到成功/失败信息 |

#### 4.2.3 审核状态页面

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"审核状态查询" |
| 状态信息区 | 显示审核状态（待审核/审核通过/审核驳回） |
| 宿舍号显示 | 审核通过后显示分配的宿舍号 |
| 驳回原因 | 审核驳回时显示驳回原因 |
| 刷新按钮 | 点击刷新审核状态 |

#### 4.2.4 宿舍管理页面（独立页面）

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"宿舍信息" |
| 宿舍信息卡片 | 显示宿舍号、楼栋、楼层、房间号、容纳人数等 |
| 室友列表 | 显示同宿舍的其他室友信息 |
| 返回按钮 | 返回报到页面 |

### 4.3 操作步骤

#### 步骤一：手机号验证登录

**步骤 1：输入手机号**
- 在"手机号"输入框中输入11位手机号码
- 必须为有效的中国大陆手机号

**步骤 2：获取验证码**
- 点击"获取验证码"按钮
- 系统会发送4位数字验证码到该手机号
- 开发阶段验证码在服务器控制台打印
- 按钮进入60秒倒计时，期间不可重复点击

**步骤 3：输入验证码**
- 在"验证码"输入框中输入收到的4位数字验证码
- 验证码有效期为5分钟

**步骤 4：登录**
- 点击"登录"按钮
- 验证成功后自动跳转到报到页面

#### 步骤二：填写报到信息

**步骤 5：填写姓名**
- 在"姓名"输入框中输入真实姓名
- 必填项，不能为空

**步骤 6：填写学号**
- 在"学号"输入框中输入学校分配的学号
- 必填项，不能为空
- 学号唯一，不可重复报到

**步骤 7：上传身份证正面照片**
- 点击"选择正面照片"按钮
- 选择身份证正面（带人像的一面）照片
- 支持 .jpg、.png 格式，大小不超过 5MB
- 系统会自动识别是否为正面照片

**步骤 8：上传身份证反面照片**
- 点击"选择反面照片"按钮
- 选择身份证反面（带国徽的一面）照片
- 支持 .jpg、.png 格式，大小不超过 5MB
- 系统会自动识别是否为反面照片

**步骤 9：提交报到**
- 确认信息填写无误后，点击"提交报到"按钮
- 按钮会显示"提交中..."，表示正在提交
- 系统会自动检测：
  - 两张照片是否为同一张
  - 正反面是否识别正确
  - 如上传顺序错误会自动纠正
- 提交成功后，页面会显示报到成功信息和报到时间

#### 步骤三：查询审核状态

**步骤 10：切换到审核状态**
- 点击"审核状态"标签
- 系统会自动查询当前审核状态

**步骤 11：查看审核结果**
- 待审核：等待管理员审核
- 审核通过：自动跳转到宿舍管理页面
- 审核驳回：显示驳回原因，可重新提交

#### 步骤四：查看宿舍信息

**步骤 12：查看宿舍详情**
- 审核通过后自动进入宿舍管理页面
- 查看宿舍号、楼栋、楼层、房间号等信息

**步骤 13：查看室友列表**
- 查看同宿舍的其他室友信息（姓名、学号）

### 4.4 表单验证

| 验证项 | 规则 | 错误提示 |
|--------|------|---------|
| 手机号 | 不能为空 | "手机号不能为空" |
| 手机号 | 必须为11位数字 | "手机号格式不正确" |
| 验证码 | 不能为空 | "请输入验证码" |
| 验证码 | 必须为4位数字 | "验证码格式不正确" |
| 验证码 | 60秒内不可重复发送 | "请等待XX秒后重试" |
| 姓名 | 不能为空 | "姓名不能为空" |
| 学号 | 不能为空 | "学号不能为空" |
| 身份证正面照片 | 必须上传 | "请上传身份证正面照片" |
| 身份证反面照片 | 必须上传 | "请上传身份证反面照片" |
| 照片格式 | .jpg 或 .png | "请上传jpg或png格式的图片" |
| 照片大小 | 不超过5MB | "图片大小不能超过5MB" |
| 照片重复 | 正反面不能为同一张 | "身份证正面和反面不能上传同一张图片" |
| 学号 | 不可重复 | "学号已存在，请勿重复报到" |

### 4.5 注意事项

1. 请确保服务器已启动后再访问网页版
2. 填写信息时请仔细核对，提交后不可修改
3. 如遇到"网络请求失败"，请检查服务器是否正常运行
4. 每个学号只能报到一次，请勿重复提交
5. 身份证照片请确保清晰可辨，系统会自动识别正反面
6. 如上传顺序错误，系统会自动纠正
7. 请勿上传同一张照片作为正反面
8. 审核通过后会自动跳转到宿舍管理页面
9. 如未分配宿舍，宿舍页面会显示"待分配"状态

---

## 5. 小程序使用指南

### 5.1 配置小程序

**开发环境配置：**

1. 打开微信开发者工具
2. 导入小程序项目（`小程序` 目录）
3. 在"详情" -> "本地设置"中勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"
4. 确保后端服务器已启动

### 5.2 页面说明

小程序包含以下页面：

#### 5.2.1 验证页面

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"新生报到系统 - 身份验证" |
| 手机号输入框 | 输入11位手机号码 |
| 获取验证码按钮 | 点击发送验证码，60秒倒计时 |
| 验证码输入框 | 输入收到的4位数字验证码 |
| 登录按钮 | 点击验证并登录 |

#### 5.2.2 报到页面

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"欢迎使用新生报到系统" |
| 手机号显示区 | 自动填充当前登录用户的手机号，不可编辑 |
| 标签切换区 | 包含"报到登记"和"审核状态"两个标签 |
| 表单区 | 包含姓名、学号输入框和身份证照片上传区域 |
| 提交按钮 | 点击提交报到信息 |
| 结果区 | 显示报到成功/失败信息 |

#### 5.2.3 宿舍管理页面（独立页面）

| 区域 | 说明 |
|------|------|
| 标题栏 | 显示"宿舍信息" |
| 宿舍信息卡片 | 显示宿舍号、楼栋、楼层、房间号、容纳人数等 |
| 室友列表 | 显示同宿舍的其他室友信息 |
| 返回按钮 | 返回报到页面 |

### 5.3 操作步骤

#### 步骤一：手机号验证登录

**步骤 1：输入手机号**
- 点击"手机号"输入框，输入11位手机号码

**步骤 2：获取验证码**
- 点击"获取验证码"按钮
- 系统会发送4位数字验证码到该手机号
- 开发阶段验证码在服务器控制台打印
- 按钮进入60秒倒计时，期间不可重复点击

**步骤 3：输入验证码**
- 点击"验证码"输入框，输入收到的4位数字验证码
- 验证码有效期为5分钟

**步骤 4：登录**
- 点击"登录"按钮
- 验证成功后自动跳转到报到页面

#### 步骤二：填写报到信息

**步骤 5：填写姓名**
- 点击"姓名"输入框，输入真实姓名

**步骤 6：填写学号**
- 点击"学号"输入框，输入学校分配的学号

**步骤 7：上传身份证正面照片**
- 点击"选择正面照片"按钮
- 从相册选择或拍照上传身份证正面（带人像的一面）照片
- 支持 .jpg、.png 格式，大小不超过 5MB
- 系统会自动识别是否为正面照片

**步骤 8：上传身份证反面照片**
- 点击"选择反面照片"按钮
- 从相册选择或拍照上传身份证反面（带国徽的一面）照片
- 支持 .jpg、.png 格式，大小不超过 5MB
- 系统会自动识别是否为反面照片

**步骤 9：提交报到**
- 确认信息填写无误后，点击"提交报到"按钮
- 按钮会显示加载状态
- 系统会自动检测正反面照片是否重复、识别是否正确
- 提交成功后会弹出"报到成功"提示

#### 步骤三：查询审核状态

**步骤 10：切换到审核状态**
- 点击"审核状态"标签
- 系统会自动查询当前审核状态

**步骤 11：查看审核结果**
- 待审核：等待管理员审核
- 审核通过：自动跳转到宿舍管理页面
- 审核驳回：显示驳回原因，可重新提交

#### 步骤四：查看宿舍信息

**步骤 12：查看宿舍详情**
- 审核通过后自动进入宿舍管理页面
- 查看宿舍号、楼栋、楼层、房间号等信息

**步骤 13：查看室友列表**
- 查看同宿舍的其他室友信息（姓名、学号）

### 5.4 注意事项

1. 小程序需在微信开发者工具中运行，或配置合法域名后在微信中运行
2. 开发环境下需关闭域名校验
3. 生产环境需配置 HTTPS 证书和合法域名
4. 审核通过后会自动跳转到宿舍管理页面

---

## 6. 管理后台使用指南

### 6.1 登录管理后台

1. 在浏览器地址栏输入：`http://127.0.0.1:8000/admin/`
2. 输入管理员用户名和密码
3. 点击"登录"

### 6.2 功能模块

| 模块 | 说明 |
|------|------|
| 学生信息 | 查看所有已报到的学生信息，支持审核操作 |
| 报到记录 | 查看所有报到记录及关联信息 |
| 宿舍信息 | 管理宿舍信息，查看入住情况 |
| 用户和组 | 管理管理员账号和权限 |

### 6.3 学生信息管理

**查看列表：**
- 点击"学生信息"进入列表页
- 显示字段：ID、姓名、学号、手机号、审核状态、宿舍号、已上传正面照片、已上传反面照片、创建时间
- 默认按创建时间倒序排列

**搜索功能：**
- 在右上角搜索框输入学号、姓名或手机号
- 点击"搜索"按钮进行筛选

**筛选功能：**
- 使用右侧"审核状态"筛选器，可按待审核/审核通过/审核驳回筛选
- 使用"创建时间"筛选器按日期范围筛选

**批量操作：**
- 勾选多个学生记录
- 从"执行操作"下拉菜单选择"审核通过选中记录"或"审核驳回选中记录"
- 点击"执行"按钮批量处理

**查看详情：**
- 点击任意学生记录进入详情页
- 可查看完整的学生信息
- 可查看上传的身份证正面和反面照片

### 6.4 宿舍信息管理

**查看列表：**
- 点击"宿舍信息"进入列表页
- 显示字段：ID、宿舍号、楼栋、楼层、房间号、容纳人数、当前入住人数、剩余床位、是否已满
- 默认按宿舍号排序

**搜索功能：**
- 在右上角搜索框输入宿舍号、楼栋或房间号
- 点击"搜索"按钮进行筛选

**筛选功能：**
- 使用右侧"楼栋"、"楼层"、"容纳人数"筛选器进行筛选

**添加宿舍：**
- 点击"添加宿舍信息"按钮
- 填写宿舍号、楼栋、楼层、房间号、容纳人数等信息
- 点击"保存"按钮

**编辑宿舍：**
- 点击任意宿舍记录进入详情页
- 修改相关信息后点击"保存"按钮

**删除宿舍：**
- 在列表页点击宿舍记录后的删除按钮
- 确认删除操作

### 6.5 报到记录管理

**查看列表：**
- 点击"报到记录"进入列表页
- 显示字段：ID、姓名、学号、报到时间
- 默认按报到时间倒序排列

**搜索功能：**
- 在右上角搜索框输入学号或姓名
- 点击"搜索"按钮进行筛选

**查看详情：**
- 点击任意报到记录进入详情页
- 可查看关联的学生信息

### 6.6 注意事项

1. 管理员账号需通过 `createsuperuser` 命令创建
2. 请勿泄露管理员账号和密码
3. 定期备份数据库文件（`db.sqlite3`）
4. 生产环境建议修改默认数据库为 MySQL 或 PostgreSQL
5. 审核通过的学生才可分配宿舍
6. 宿舍已满时无法继续分配

---

## 7. 常见问题

### 7.1 安装部署问题

**Q1：运行 `python manage.py runserver` 报错 "No module named django"**

A：未安装Django，请运行以下命令安装：
```bash
pip install django django-cors-headers opencv-python numpy pillow
```

**Q2：运行 `python manage.py migrate` 报错**

A：请确保当前目录为项目根目录（包含 `manage.py` 文件的目录）

**Q3：服务器启动后无法访问**

A：请检查：
1. 端口8000是否被占用
2. 防火墙是否阻止访问
3. 服务器是否成功启动（查看终端输出）

**Q4：如何更换端口？**

A：使用以下命令指定端口：
```bash
python manage.py runserver 8080
```

### 7.2 网页版问题

**Q5：访问网页版显示404**

A：请确保访问的是根路径 `/`，而不是其他路径

**Q6：点击"提交报到"按钮后显示"网络请求失败"**

A：请检查：
1. Django服务器是否正在运行
2. 浏览器控制台是否有错误信息
3. 网络连接是否正常

**Q7：提交后显示"学号已存在"**

A：该学号已经报到过，请勿重复提交

**Q8：提示"未检测到身份证正面照片"**

A：请检查上传的照片是否为身份证正面（带人像的一面），确保照片清晰可辨

**Q9：提示"未检测到身份证反面照片"**

A：请检查上传的照片是否为身份证反面（带国徽的一面），确保照片清晰可辨

**Q10：提示"身份证正面和反面不能上传同一张图片"**

A：请确保正面和反面分别上传了不同的照片

**Q11：审核通过后页面没有跳转**

A：请刷新页面重试，或检查网络连接

### 7.3 小程序问题

**Q12：小程序请求报错 "request:fail url not in domain list"**

A：请在微信开发者工具中勾选"不校验合法域名"

**Q13：小程序无法连接服务器**

A：请确保：
1. 后端服务器已启动
2. 小程序中的服务器地址正确
3. 手机和电脑在同一网络环境下（真机调试时）

**Q14：审核通过后没有跳转到宿舍页面**

A：请刷新页面重试，或检查网络连接

### 7.4 管理后台问题

**Q15：忘记管理员密码**

A：可通过以下命令重置：
```bash
python manage.py changepassword 用户名
```

**Q16：如何创建新的管理员账号？**

A：运行以下命令：
```bash
python manage.py createsuperuser
```

**Q17：如何查看学生上传的身份证照片？**

A：进入学生信息详情页，点击照片链接即可查看

**Q18：如何为学生分配宿舍？**

A：进入学生信息详情页，在"宿舍号"字段输入宿舍号，点击"保存"

**Q19：宿舍管理页面打不开**

A：请检查数据库迁移是否已执行，确保 `Dormitory` 模型已正确创建

### 7.5 数据库问题

**Q20：如何备份数据库？**

A：复制 `新生报到系统/db.sqlite3` 文件到其他位置

**Q21：如何恢复数据库？**

A：将备份的 `db.sqlite3` 文件复制回 `新生报到系统/` 目录

**Q22：如何导出报到数据？**

A：可通过管理后台查看，或使用以下命令导出：
```bash
python manage.py dumpdata api > backup.json
```

---

## 8. 技术支持

### 8.1 项目地址

- GitHub仓库：[https://github.com/sxr-star/sxr](https://github.com/sxr-star/sxr)
- 分支：main

### 8.2 文档清单

| 文档 | 说明 |
|------|------|
| 需求分析报告.md | 系统需求分析 |
| 技术方案.md | 技术架构方案 |
| 数据库设计文档.md | 数据库设计说明 |
| 测试报告.md | 系统测试报告 |
| 使用说明书（本文档） | 系统使用指南 |

### 8.3 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-04-27 | 最小可用版本，仅跑通链路 |
| 2.0 | 2026-04-27 | 信息采集版，支持学生信息录入 |
| 3.0 | 2026-04-27 | 手机号验证版，支持验证码登录和身份证照片上传 |
| 4.0 | 2026-05-22 | 审核分配版，新增管理员审核、宿舍分配和数据统计功能 |

### 8.4 联系方式

如有问题，请联系项目开发人员。

---

**文档版本：** 4.0  
**更新日期：** 2026-05-22  
**编写人员：** AI Assistant