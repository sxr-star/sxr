# 新生报到系统 2.0 - 测试报告

## 1. 测试概述

### 1.1 测试目的

验证新生报到系统 2.0 版本（信息采集版）的各项功能是否正常运行，确保从前端表单到后端 API 到数据库存储的完整数据链路畅通，学生信息采集与报到记录关联功能正确实现。

### 1.2 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows |
| Python 版本 | 3.13.2 |
| Django 版本 | 6.0.4 |
| 数据库 | SQLite 3 |
| 浏览器 | Chrome / Edge |
| 测试日期 | 2026-04-27 |

### 1.3 测试范围

| 模块 | 说明 |
|------|------|
| 后端 API | 新增学生信息采集接口功能测试 |
| 网页版前端 | 表单填写与提交功能测试 |
| 小程序前端 | 表单填写与提交功能测试 |
| 数据库 | 学生信息表与报到记录表关联测试 |
| 管理后台 | Django Admin 学生信息管理功能测试 |
| 数据校验 | 前端与后端双重校验测试 |

---

## 2. 后端 API 测试

### 2.1 接口信息

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 首页 | GET | `/` | 返回网页版前端页面 |
| 报到接口（1.0） | POST | `/api/register/` | 接收报到请求，创建操作记录 |
| 信息采集报到接口（2.0新增） | POST | `/api/register_with_info/` | 接收学生信息并创建报到记录 |

### 2.2 测试用例

#### 测试用例 1：首页访问

| 项目 | 内容 |
|------|------|
| 测试步骤 | 浏览器访问 `http://127.0.0.1:8000/` |
| 预期结果 | 返回网页版前端页面，显示表单（姓名、学号、手机号）和"提交报到"按钮 |
| 实际结果 | ✅ 通过 |
| 备注 | 页面正常渲染，表单样式和布局正确 |

#### 测试用例 2：信息采集接口 - 正常请求

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 POST 请求到 `/api/register_with_info/`，携带 JSON 数据：`{"name": "张三", "student_id": "2024001001", "phone": "13800138000"}` |
| 预期结果 | 返回 `{"success": true, "message": "报到成功", "data": {...}}`，数据库中学生信息表和报到记录表各新增一条记录 |
| 实际结果 | ✅ 通过 |
| 备注 | 接口响应正常，数据正确写入数据库，关联关系正确 |

#### 测试用例 3：信息采集接口 - 姓名为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 POST 请求，`name` 字段为空 |
| 预期结果 | 返回 `{"success": false, "message": "姓名不能为空"}` |
| 实际结果 | ✅ 通过 |
| 备注 | 后端校验正确 |

#### 测试用例 4：信息采集接口 - 学号为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 POST 请求，`student_id` 字段为空 |
| 预期结果 | 返回 `{"success": false, "message": "学号不能为空"}` |
| 实际结果 | ✅ 通过 |
| 备注 | 后端校验正确 |

#### 测试用例 5：信息采集接口 - 手机号为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 POST 请求，`phone` 字段为空 |
| 预期结果 | 返回 `{"success": false, "message": "手机号不能为空"}` |
| 实际结果 | ✅ 通过 |
| 备注 | 后端校验正确 |

#### 测试用例 6：信息采集接口 - 手机号格式错误

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 POST 请求，`phone` 字段为 "123" |
| 预期结果 | 返回 `{"success": false, "message": "手机号格式不正确"}` |
| 实际结果 | ✅ 通过 |
| 备注 | 后端校验正确，检查长度为11位且全为数字 |

#### 测试用例 7：信息采集接口 - 学号重复

| 项目 | 内容 |
|------|------|
| 测试步骤 | 使用已存在的学号发送 POST 请求 |
| 预期结果 | 返回 `{"success": false, "message": "学号已存在，请勿重复报到"}` |
| 实际结果 | ✅ 通过 |
| 备注 | 数据库唯一约束生效，后端捕获 IntegrityError |

#### 测试用例 8：信息采集接口 - GET 请求（应被拒绝）

| 项目 | 内容 |
|------|------|
| 测试步骤 | 发送 GET 请求到 `/api/register_with_info/` |
| 预期结果 | 返回 405 Method Not Allowed |
| 实际结果 | ✅ 通过 |
| 备注 | 接口使用了 `@require_POST` 装饰器，正确拒绝非 POST 请求 |

---

## 3. 网页版前端测试

### 3.1 页面元素

| 元素 | 说明 | 状态 |
|------|------|------|
| 标题栏 | 显示"欢迎使用新生报到系统" | ✅ 正常 |
| 姓名输入框 | 用于输入学生姓名 | ✅ 正常 |
| 学号输入框 | 用于输入学生学号 | ✅ 正常 |
| 手机号输入框 | 用于输入学生手机号 | ✅ 正常 |
| 提交按钮 | 显示"提交报到" | ✅ 正常 |
| 错误提示区 | 显示字段校验错误信息 | ✅ 正常 |
| 结果提示区 | 显示报到成功/失败信息 | ✅ 正常 |
| 报到信息展示区 | 显示成功报到后的详细信息 | ✅ 正常 |

### 3.2 功能测试

#### 测试用例 9：表单填写与提交

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 打开网页版首页 2. 填写姓名、学号、手机号 3. 点击"提交报到"按钮 |
| 预期结果 | 1. 按钮文字变为"提交中..." 2. 按钮禁用 3. 请求发送后显示"报到成功" 4. 显示报到详细信息 5. 表单自动清空 |
| 实际结果 | ✅ 通过 |
| 备注 | 表单提交流程完整，用户体验良好 |

#### 测试用例 10：前端校验 - 姓名为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 不填写姓名，直接点击"提交报到" |
| 预期结果 | 姓名输入框变红，显示"姓名不能为空"错误提示，不发送请求 |
| 实际结果 | ✅ 通过 |
| 备注 | 前端校验正确 |

#### 测试用例 11：前端校验 - 学号为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 不填写学号，直接点击"提交报到" |
| 预期结果 | 学号输入框变红，显示"学号不能为空"错误提示，不发送请求 |
| 实际结果 | ✅ 通过 |
| 备注 | 前端校验正确 |

#### 测试用例 12：前端校验 - 手机号格式错误

| 项目 | 内容 |
|------|------|
| 测试步骤 | 手机号输入"123"，点击"提交报到" |
| 预期结果 | 手机号输入框变红，显示"手机号格式不正确"错误提示，不发送请求 |
| 实际结果 | ✅ 通过 |
| 备注 | 前端校验正确，检查长度为11位且全为数字 |

#### 测试用例 13：防重复点击

| 项目 | 内容 |
|------|------|
| 测试步骤 | 快速多次点击"提交报到"按钮 |
| 预期结果 | 只发送一次请求，不会重复提交 |
| 实际结果 | ✅ 通过 |
| 备注 | 使用 `isLoading` 标志防止重复提交 |

#### 测试用例 14：服务器未启动时的错误处理

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 不启动 Django 服务器 2. 填写表单并点击"提交报到" |
| 预期结果 | 显示"网络请求失败，请确保服务器正在运行" |
| 实际结果 | ✅ 通过 |
| 备注 | 前端有完善的错误捕获机制 |

---

## 4. 小程序前端测试

### 4.1 页面元素

| 元素 | 说明 | 状态 |
|------|------|------|
| 标题栏 | 显示"欢迎使用新生报到系统" | ✅ 正常 |
| 姓名输入框 | 用于输入学生姓名 | ✅ 正常 |
| 学号输入框 | 用于输入学生学号 | ✅ 正常 |
| 手机号输入框 | 用于输入学生手机号 | ✅ 正常 |
| 提交按钮 | 显示"提交报到" | ✅ 正常 |
| 错误提示 | 显示字段校验错误信息 | ✅ 正常 |
| 结果提示 | 显示报到成功/失败信息 | ✅ 正常 |
| 报到信息展示 | 显示成功报到后的详细信息 | ✅ 正常 |

### 4.2 功能测试

#### 测试用例 15：表单填写与提交

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 打开小程序页面 2. 填写姓名、学号、手机号 3. 点击"提交报到"按钮 |
| 预期结果 | 1. 按钮显示加载状态 2. 请求发送后显示"报到成功" 3. 显示报到详细信息 4. 表单自动清空 5. 弹出成功提示 |
| 实际结果 | ✅ 通过 |
| 备注 | 表单提交流程完整，wx.showToast 提示正确 |

#### 测试用例 16：前端校验 - 字段为空

| 项目 | 内容 |
|------|------|
| 测试步骤 | 不填写任意字段，直接点击"提交报到" |
| 预期结果 | 对应字段显示错误提示，不发送请求 |
| 实际结果 | ✅ 通过 |
| 备注 | 前端校验正确 |

#### 测试用例 17：前端校验 - 手机号格式错误

| 项目 | 内容 |
|------|------|
| 测试步骤 | 手机号输入"123"，点击"提交报到" |
| 预期结果 | 显示"手机号格式不正确"错误提示，不发送请求 |
| 实际结果 | ✅ 通过 |
| 备注 | 前端校验正确 |

---

## 5. 数据库测试

### 5.1 数据模型

#### 学生信息表（api_studentinfo）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | AutoField | 主键，自增 |
| name | CharField(50) | 学生姓名 |
| student_id | CharField(20) | 学号，唯一约束 |
| phone | CharField(11) | 手机号 |
| created_at | DateTimeField | 创建时间，自动记录 |

#### 报到记录表（api_registrationrecord）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | AutoField | 主键，自增 |
| student | OneToOneField | 关联学生信息表 |
| register_time | DateTimeField | 报到时间，自动记录 |

### 5.2 测试用例

#### 测试用例 18：学生信息数据写入

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 调用信息采集接口 2. 查询学生信息表 |
| 预期结果 | 数据库新增一条 StudentInfo 记录 |
| 实际结果 | ✅ 通过 |
| 备注 | 数据正确写入 SQLite 数据库 |

#### 测试用例 19：报到记录数据写入

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 调用信息采集接口 2. 查询报到记录表 |
| 预期结果 | 数据库新增一条 RegistrationRecord 记录，且 student 字段关联到对应的学生信息 |
| 实际结果 | ✅ 通过 |
| 备注 | 一对一关联正确 |

#### 测试用例 20：学号唯一性约束

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 使用学号 "2024001001" 提交报到 2. 再次使用相同学号提交 |
| 预期结果 | 第二次提交失败，返回"学号已存在"错误 |
| 实际结果 | ✅ 通过 |
| 备注 | 数据库唯一约束生效 |

#### 测试用例 21：数据查询与排序

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在 Django Admin 中查看学生信息和报到记录 |
| 预期结果 | 显示所有记录，按时间倒序排列 |
| 实际结果 | ✅ 通过 |
| 备注 | 模型配置了 `ordering = ['-created_at']` 和 `ordering = ['-register_time']` |

---

## 6. 管理后台测试

### 6.1 测试用例

#### 测试用例 22：Admin 登录

| 项目 | 内容 |
|------|------|
| 测试步骤 | 访问 `http://127.0.0.1:8000/admin/` 并登录 |
| 预期结果 | 成功进入管理后台 |
| 实际结果 | ✅ 通过 |
| 备注 | 使用 `createsuperuser` 创建的管理员账号 |

#### 测试用例 23：查看学生信息

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在管理后台点击"学生信息" |
| 预期结果 | 显示所有学生信息列表（ID、姓名、学号、手机号、创建时间） |
| 实际结果 | ✅ 通过 |
| 备注 | 列表显示完整，支持搜索和筛选 |

#### 测试用例 24：查看报到记录

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在管理后台点击"报到记录" |
| 预期结果 | 显示所有报到记录列表（ID、姓名、学号、报到时间） |
| 实际结果 | ✅ 通过 |
| 备注 | 通过自定义方法显示关联学生的姓名和学号 |

#### 测试用例 25：按学号搜索

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在学生信息列表中使用搜索框输入学号 |
| 预期结果 | 显示匹配的学生信息 |
| 实际结果 | ✅ 通过 |
| 备注 | 搜索功能正常 |

---

## 7. 数据库迁移测试

### 7.1 测试用例

#### 测试用例 26：数据库迁移

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 执行 `python manage.py makemigrations api` 2. 执行 `python manage.py migrate` |
| 预期结果 | 1. 生成 0002_studentinfo_registrationrecord.py 迁移文件 2. 新表在数据库中创建成功 |
| 实际结果 | ✅ 通过 |
| 备注 | 迁移过程顺利，无错误 |

#### 测试用例 27：数据备份

| 项目 | 内容 |
|------|------|
| 测试步骤 | 迁移前备份 db.sqlite3 文件 |
| 预期结果 | 备份文件创建成功 |
| 实际结果 | ✅ 通过 |
| 备注 | 备份文件为 db.sqlite3.backup_2.0 |

---

## 8. 测试总结

### 8.1 测试结果统计

| 类别 | 总数 | 通过 | 失败 | 通过率 |
|------|------|------|------|--------|
| 后端 API | 8 | 8 | 0 | 100% |
| 网页版前端 | 6 | 6 | 0 | 100% |
| 小程序前端 | 3 | 3 | 0 | 100% |
| 数据库 | 4 | 4 | 0 | 100% |
| 管理后台 | 4 | 4 | 0 | 100% |
| 数据库迁移 | 2 | 2 | 0 | 100% |
| **合计** | **27** | **27** | **0** | **100%** |

### 8.2 测试结论

新生报到系统 2.0 版本（信息采集版）各项功能均正常运行，从前端表单到后端 API 到数据库存储的完整数据链路已打通，学生信息采集与报到记录关联功能正确实现，满足 2.0 版本的需求要求。

### 8.3 已知限制

| 序号 | 说明 |
|------|------|
| 1 | 当前版本仅实现基础信息采集，未包含图片上传功能 |
| 2 | 未接入微信登录，采用匿名标识代替用户身份 |
| 3 | 仅支持开发环境运行，未配置生产环境部署 |
| 4 | 小程序版需要在微信开发者工具中单独测试（需配置不校验合法域名） |
| 5 | 未实现管理员审核流程 |

---

## 9. 测试人员

| 项目 | 内容 |
|------|------|
| 测试人员 | 苏鑫茹 |
| 审核人员 | - |
| 报告日期 | 2026-04-27 |
